## ❓ 10) `docs/FAQ.md` (common problems)

I’ll include real error cases from your code paths (detector failures, missing `app.module.ts`, ORM conflicts, Docker merge issues, OAuth misconfig, Prisma connection issues, etc.) with copy/paste fixes.

---
