## 🔁 9) `docs/ROLLBACK.md` (restore points)

- What triggers a restore point (before install, after failure)
- What’s tracked: generated files, modified files, added dependencies/devDependencies
- Manifest format + backup directory structure
- Exact `rollback` behavior + edge cases
- Manual recovery steps if something goes wrong

---
