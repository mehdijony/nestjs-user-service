## 🐳 7) `docs/DOCKER.md` (Docker Compose)

- Services generated by feature selection (Postgres/MySQL/Mongo, Redis, RabbitMQ, Kafka, Prometheus/Grafana)
- Volumes and networks
- Environment variables + `.env` mapping
- Commands: `up`, `down`, `logs`, rebuilding, database-only startup

---
