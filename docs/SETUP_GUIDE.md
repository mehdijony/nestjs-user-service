## 🧪 3) `docs/SETUP_GUIDE.md` (step-by-step)

I’ll write a detailed guide that covers:

- ✅ Prerequisites + environment setup
- ✅ `user-service-generator init` with sample prompts and expected outputs
- ✅ `user-service-setup init` with detection summary, what gets injected, what gets appended
- ✅ `.env` keys generated per feature (JWT, OAuth, Redis, email/SMS, S3, etc.)
- ✅ Database steps per ORM (Prisma migrate/push, TypeORM migrations, Mongoose indexes)
- ✅ Running locally + Swagger + health checks
- ✅ Common errors + fixes (with exact error messages you’ll see)

_(I’ll tailor the exact prompts and file paths from your repo’s `prompts.ts`, `installer.ts`, `project.detector.ts`, etc.)_

---
